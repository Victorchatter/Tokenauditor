__pycache__/
*.pyc
*.egg-info/
build/
dist/
.venv/
tmp_transcript.json
.playwright-mcp/
# dev-only chart preview (the shipped charts docs/bar.svg + docs/area.svg are committed)
docs/_chart_preview.html
chart_preview.png
# engramkit init writes per-vendor agent config into every repo it touches.
# Machine-local tooling, not project config — and a committed .claude/settings.json
# runs a SessionStart hook on the machine of anyone who clones.
.mcp.json
.cursor/
.gemini/
.codex/
.claude/settings.json
.claude/settings.local.json
